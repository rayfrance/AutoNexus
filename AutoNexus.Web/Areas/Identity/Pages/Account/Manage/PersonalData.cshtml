@page
@model PersonalDataModel
@{
    Layout = "_ManageLayout";
    ViewData["Title"] = "Dados Pessoais";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3>@ViewData["Title"]</h3>

<div class="row">
    <div class="col-md-8">
        <p>
            Sua conta contém dados pessoais que você nos forneceu. Esta página permite que você baixe ou exclua esses dados.
        </p>
        <p>
            <strong>A exclusão destes dados removerá permanentemente sua conta e ela não poderá ser recuperada.</strong>
        </p>

        <div class="d-flex gap-2 mt-4">

            <form id="download-data" asp-page="DownloadPersonalData" method="post" class="d-flex">
                <button class="btn btn-primary d-flex align-items-center" type="submit">
                    Baixar Dados
                </button>
            </form>

            <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger d-flex align-items-center">
                Excluir Conta
            </a>

        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}